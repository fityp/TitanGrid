{
  "name": "titangrid-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @titangrid/playground",
    "test": "npm run test -w @titangrid/core && npm run test -w titangrid",
    "test:watch": "npm run test:watch -w @titangrid/core",
    "build": "npm run build -w titangrid && npm run build -w @titangrid/playground"
  },
  "engines": {
    "node": ">=20"
  }
}
